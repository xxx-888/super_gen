# ============================================================
# SceneGen 后端依赖清单
# Python 3.14+ / FastAPI / SQLAlchemy 2.0 (async)
# ============================================================

# ---- Web 框架 ----
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9        # 表单 / 文件上传
email-validator>=2.1           # pydantic EmailStr 校验

# ---- 数据库 (异步) ----
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29                  # PostgreSQL 异步驱动 (当前缺失的包)
alembic>=1.13                  # 数据库迁移

# ---- 认证 / 安全 ----
pydantic>=2.6
pydantic-settings>=2.2
python-jose[cryptography]>=3.3 # JWT
passlib[bcrypt]>=1.7
# 注意: passlib 1.7.4 与 bcrypt>=4.1 不兼容 (缺少 __about__ 属性会触发 72 字节报错)
# 必须锁定 4.0.1，否则注册/登录的密码哈希会失败
bcrypt==4.0.1

# ---- 缓存 / 任务队列 ----
redis>=5.0
celery>=5.3

# ---- 文件 / 媒体处理 ----
aiofiles>=23.2                 # 异步文件读写
Pillow>=10.2                   # 图片处理
python-docx>=1.1               # Word 文档解析（剧本上传）
pypdf>=4.0                     # PDF 文档解析（剧本上传）

# ---- 外部 API 调用 ----
httpx>=0.27                    # 调用 AI 模型 API (同步/异步); Agent LLM 客户端也用 httpx 直连 OpenAI 兼容接口
# 注: Agent 模式的 LLM 默认走智谱 GLM 的 OpenAI 兼容端点，无需额外 SDK；
#     如需用官方 SDK，可选装 zhipuai>=2.0 或 openai>=1.0（当前实现不依赖）

# ---- 工具 ----
tenacity>=8.2                  # 重试装饰器
python-dotenv>=1.0
